<template>
  <div class="max-w-2xl mx-auto px-4 text-center" style="padding-top: calc(6rem + env(safe-area-inset-top)); padding-bottom: max(6rem, calc(6rem + env(safe-area-inset-bottom)))">
    <div class="text-4xl mb-4">⚡</div>
    <h1 class="text-xl font-black text-white mb-2">Live Results</h1>
    <p class="text-sm text-zinc-400">Live match data, group standings, and prop results will appear here once the tournament begins.</p>
  </div>
</template>
